作业：
动态内存申请，动态数组。

------------------------
std::cin >> n;

int* p = new int[n]; // 堆上申请的

delete[] p; // memory leak

double result = (double) (sum) / n;

------------------------

自己去实现大数的逻辑，限制就是内存空间.

------------------------

你写业务的时候，尽量避免，用名称意义非常清楚的函数来表示你的业务逻辑。

用于标准库，boost，开源的，行业里面的大库，你会用，你明白它是运算重载的就可以了，我们尽量从 OOP 的角度去想问题。

------------------------

模板.................容器，迭代器，仿函数，算法

vector: 随机访问，你给下标，一步到位。

