作业：

明白多态是为了解决什么问题？

引用B 解释 A，同学们连B 都不知道，C 来解释 B。

A，在 A 所处的环境里面去讲，同学以什么为标准呢，你知道它解决了什么问题。

多态这种行为在 C++中实现的方式有些不自然，在 Python 中是语言内置的，默认的。

在 Python里面是语言内置的，用 Python 的时候就有一种比较自由的感觉，像在用自然语言。

-----------------------------------------------------------------------

我推荐的是用 clion.

想用其他的环境也可以，只要能够顺利进行 C++的学习就可以了。

vim/neovim/emacs 命令行的编辑界面里面，需要你去自行配置插件，用插件来实现语法高亮和自动补全。

YCM，YouCompleteMe. 你如果用的是 Windows 的会非常不方便。

-----------------------------------------------------------------------

turbo c++, vc++, vi/vim

vi/vim + 远程的开发机。

vscode为生态搭建的开发环境，根据企业的要求去调整你的环境。

-----------------------------------------------------------------------

多态用的多不多。

-----------------------------------------------------------------------

纯虚函数就是用来定义一个抽象的接口, 

-----------------------------------------------------------------------


比较难的问题，用语言这个工具来解决比较难的问题，而不是去研究比较难的工具。意图搞清楚。
把昨天上课的时候讲的 2-8 原则也想一想

-----------------------------------------------------------------------

class Test {
public:
	Test(xx, xxx){ // 普通的构造

	}

	Test(const Test& test) { // copy constructor

	}

private:
    xxxxx
};

int main() {
	Test test1;
	Test test2(test1); // Test test2 = test1
}


默认都是浅 copy，当我们发现浅 copy 不满足业务要求的时候，需要进行深copy

实际的开发中，我们是用良好的约束规则来规避这些问题，我都去定义一个深copy.


class Test {
public:
	int a;
	int b;	
	Test(const Test& t) { // 这就是我的意图，我只 copy 的感兴趣的
		this->a = t.a;
	}
};


---------------------------------------------------------
最开始的时候 C++标准里面是没有智能指针的，和 C 语言一样的原始指针。
