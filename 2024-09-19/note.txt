大多数的时候，其实没有 GPU 资源给你去部署，

算法工程师是有的，

算法工程，部署也是你做：

1. model.eval(), 加上网络请求的代码, demo一下。

2. ONNX，ONNX runtime（Python部署), C++, (GPU上的)，openvino(CPU)

3. TensorRT

4. ASIC，FPGA (完全的定制化)

---------------------------------------

大部分的工程，如果要实现跨语言的沟通，几乎都会导出为 C 语言的 ABI，

C++ 也要导出 C 语言的 ABI

Python要调用一个扩展的模块，调用 C 的模型，就算你这个模块是用 C++写的，你也要导出为 c 的 ABI。

---------------------------------------

